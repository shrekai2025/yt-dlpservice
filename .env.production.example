# 生产环境配置示例
# 复制此文件为 .env.production 或 .env.local，然后修改相应的值

NODE_ENV=production

# Cookie 安全设置
# 如果远程服务器使用 HTTP（不推荐），设置为 false
# 如果使用 HTTPS（推荐），删除此行或设置为 true
FORCE_SECURE_COOKIE=false

# 数据库配置（如果使用 PostgreSQL）
# DATABASE_URL="postgresql://user:password@localhost:5432/dbname"

# 如果使用 SQLite（默认）
DATABASE_URL="file:./prisma/dev.db"

# 其他环境变量...
